# Corrected threshold revision-v2 results

The canonical local outputs are the CSV files generated by `threshold_revision_v2.py`. GitHub browser upload cannot create objects for repository paths configured for Git LFS, so the fork contains unchanged JSON serializations of each result table for review. The JSON mirrors are not recomputed or rounded beyond JSON serialization.

The corrected analysis uses nested station-grouped cross-validation and evaluates the locked test once after training-only threshold selection. The frozen conclusion is that no stable threshold is identified.
